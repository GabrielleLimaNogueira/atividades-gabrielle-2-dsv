Algoritmo "correcaoex2"

Var
 cotacao, vdolar, vreal : real

Inicio
escreval("Digite a cotação atual *DOLAR")
leia(cotacao)

 escreval("Digite o valoe em dolar é")
 leia(vdolar)
 
 vreal <- vdolar * cotacao
 
 escreval("o valor em reais é",vreal)

Fimalgoritmo